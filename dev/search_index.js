var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#Public-Interface","page":"Public Interface","text":"","title":"Public Interface"},{"category":"module","location":"api/#HDF5Vectors.HDF5Vectors","page":"Public Interface","text":"This module implements an AbstractVector whose underlying data is stored in an HDF5 file.\n\nIt generally can store vectors of elements with fixed sizes, where that element is one of:\n\nHDF5-compatible numeric type (signed/unsigned ints and floats)\nEnum\nSVector, SMatrix, and SArray of numeric types\nTuple of numeric types\nbits-type consisting of any types on this list\ngeneral composite type consisting of any types on this list\nVector, Matrix, and Array of any types on this list, as long as their dimensions are always the same\nString\nreconstructible singleton types\n\nFurther, it can serialize types to bytes or strings and store those in the HDF5 file. This allows it to store:\n\nCustom types explicitly assigned a serialization storage style\nVector, Matrix, and Array values whose dimensions are not declared or vary between elements\n\nIt fulfills the general AbstractVector interface. Note, however, that iterating directly is slow; for far better speed, iterate on iterable(arr).\n\n\n\n\n\n","title":"HDF5Vectors.HDF5Vectors"},{"category":"type","location":"api/#HDF5Vectors.AbstractHDF5Vector","page":"Public Interface","text":"An abstract type intended as the parent for all type of HDF5 vectors. Subtypes should have a corresponding storage style and implement create_hdf5_vector, and at least these parts of the AbstractArray interface: length, push!, getindex, and collect. setindex! is optional for storage that supports in-place replacement.\n\n\n\n\n\n","title":"HDF5Vectors.AbstractHDF5Vector"},{"category":"type","location":"api/#HDF5Vectors.AbstractHDF5VectorStorageStyle","page":"Public Interface","text":"An abstract type intended as a parent for all HDF5 vector storage styles.\n\n\n\n\n\n","title":"HDF5Vectors.AbstractHDF5VectorStorageStyle"},{"category":"type","location":"api/#HDF5Vectors.CompositeStorageStyle","page":"Public Interface","text":"Used to store composite structured information, like:\n\nGeneral tuple of types on this list\nGeneral named tuple of types on this list\nGeneral struct of types on this list\n\n\n\n\n\n","title":"HDF5Vectors.CompositeStorageStyle"},{"category":"type","location":"api/#HDF5Vectors.ElementalStorageStyle","page":"Public Interface","text":"Used to store \"elemental\" types – types that HDF5 can natively understand, including:\n\nInt8, Int16, Int32, and Int64 (and unsigned forms)\nFloat32 and Float64\nEnum\nChar\nBits-type structs\nString\n\n\n\n\n\n","title":"HDF5Vectors.ElementalStorageStyle"},{"category":"type","location":"api/#HDF5Vectors.SingletonStorageStyle","page":"Public Interface","text":"Used for singleton types whose value can be reconstructed from the element type. Only the vector length is stored because the element type determines every value.\n\n\n\n\n\n","title":"HDF5Vectors.SingletonStorageStyle"},{"category":"function","location":"api/#HDF5Vectors.construct","page":"Public Interface","text":"construct(type::Type, el)\n\nCreate the appropriate Julia value from the raw element el retrieved from the HDF5 dataset. The behaviour is determined by the storage style associated with type. This generic definition is a placeholder; concrete storage implementations overload construct for their particular container types and element representations.\n\n\n\n\n\n","title":"HDF5Vectors.construct"},{"category":"method","location":"api/#HDF5Vectors.copy_to_hdf5_vector-Tuple{Any, Any, Any}","page":"Public Interface","text":"copy_to_hdf5_vector(\n    group,\n    name,\n    collection;\n    dims = nothing,\n    chunk_length = 1000,\n    portable = true,\n)\n\nCreates an HDF5 vector using eltype(collection) and copies the collection into it. The copy uses specialized bulk writes where the selected storage format supports them.\n\nOptional keyword arguments:\n\ndims = nothing: Dimensions of each dynamically sized array element. This must be a tuple of positive integers whose length matches the array rank. It enables efficient array-like storage for arrays of elemental values; dimensions of tuples and static arrays are inferred from their types. Dimensions are not inferred by inspecting the collection.\nchunk_length = 1000: Positive integer chunk length for the underlying HDF5 datasets. It affects storage layout and I/O performance but does not limit the vector length.\nportable = true: When true, use field-oriented storage for composite bits types. When false, permit a faster native HDF5 datatype where available. It is ignored for types with only one supported representation and does not make Julia-serialized data portable.\n\n\n\n\n\n","title":"HDF5Vectors.copy_to_hdf5_vector"},{"category":"method","location":"api/#HDF5Vectors.create_hdf5_vector-Tuple{Any, Any, Any}","page":"Public Interface","text":"create_hdf5_vector(\n    group,\n    name,\n    el_type;\n    dims = nothing,\n    chunk_length = 1000,\n    portable = true,\n)\n\nCreates the appropriate HDF5 vector type for the given element type, storing the vector in the given HDF5 group under name.\n\nOptional keyword arguments:\n\ndims = nothing: Dimensions of each dynamically sized array element. This must be a tuple of positive integers whose length matches the array rank. It enables efficient array-like storage for arrays of elemental values; dimensions of tuples and static arrays are inferred from their types.\nchunk_length = 1000: Positive integer chunk length for the underlying HDF5 datasets. It affects storage layout and I/O performance but does not limit the vector length.\nportable = true: When true, use field-oriented storage for composite bits types. When false, permit a faster native HDF5 datatype where available. It is ignored for types with only one supported representation and does not make Julia-serialized data portable.\n\n\n\n\n\n","title":"HDF5Vectors.create_hdf5_vector"},{"category":"method","location":"api/#HDF5Vectors.create_hdf5_vector-Tuple{HDF5Vectors.AbstractHDF5VectorStorageStyle, Any, Any, Any}","page":"Public Interface","text":"create_hdf5_vector(style, group, name, el_type; kwargs...)\n\nCreates the appropriate HDF5 vector type for the given storage style and element type, storing the vector in the given HDF5 group under name. This is an implementation hook for storage backends; users should select a style by defining storage_style for their element type and call the overload without a style.\n\n\n\n\n\n","title":"HDF5Vectors.create_hdf5_vector"},{"category":"function","location":"api/#HDF5Vectors.deconstruct","page":"Public Interface","text":"deconstruct(type::Type, el)\n\nConvert the Julia value el into the representation stored in the HDF5 file. The storage style associated with type chooses how the conversion is performed. Like construct, this generic definition is a no-op; storage backends provide concrete methods.\n\n\n\n\n\n","title":"HDF5Vectors.deconstruct"},{"category":"method","location":"api/#HDF5Vectors.iterable-Tuple{AbstractHDF5Vector}","page":"Public Interface","text":"iterable(arr::AbstractHDF5Vector)\n\nReturns an iterable type corresponding to the given HDF5 vector. This is generally much faster than iterating on the vector directly. That is, instead of [f(el) for el in arr], it is much faster to use [f(el) for el in iterable(arr)].\n\n\n\n\n\n","title":"HDF5Vectors.iterable"},{"category":"method","location":"api/#HDF5Vectors.load_hdf5_vector-Tuple{Any, Any}","page":"Public Interface","text":"load_hdf5_vector(group_or_dataset, el_type; kwargs...)\n\nReconstruct an HDF5 vector when the caller already knows the element type. This overload is primarily used when loading a dataset directly (rather than the parent group) or when the metadata does not reside in the expectation of the vector type.  The element type is passed explicitly and used to select the storage style; the remainder of the arguments is forwarded.\n\n\n\n\n\n","title":"HDF5Vectors.load_hdf5_vector"},{"category":"method","location":"api/#HDF5Vectors.load_hdf5_vector-Tuple{Any}","page":"Public Interface","text":"load_hdf5_vector(group; kwargs...)\n\nReconstruct an HDF5 vector from a group created by create_hdf5_vector.  The metadata stored in the group (type, dimensions, portability) is used to determine which specific vector implementation to instantiate.  This form takes only the group and pulls the element type from the metadata; the optional keyword arguments are forwarded to storage_style and to the underlying loader.\n\n\n\n\n\n","title":"HDF5Vectors.load_hdf5_vector"},{"category":"method","location":"api/#HDF5Vectors.storage_style-Tuple{Type}","page":"Public Interface","text":"storage_style(el_type::Type; kwargs...)\n\nReturns the storage style intended for this type. Available styles include:\n\nElementalStorageStyle for scalars or non-portable bits-type structs\nSingletonStorageStyle for types that have exactly one possible value\nArrayStorageStyle for arrays of known, consistent dimensions holding elemental types\nCompositeStorageStyle for general structs\nByteArrayStorageStyle for Julia serialization\nJSONStorageStyle for serializing types to JSON strings\n\nThe default storage style for scalars and \"non-portable\" bits-type structs (more below) is ElementalStorageStyle. For vectors with known dimensions, ArrayStorageStyle is the default. Singleton types use SingletonStorageStyle. For other structs (either non-bits-types or \"portable\"), the default is CompositeStorageStyle. Nonconcrete types and arrays without known dimensions default to ByteArrayStorageStyle. Unsupported primitive types produce an error unless the user explicitly defines another storage style.\n\nThe storage style is selected again from the element type and stored options when a vector is loaded. A custom storage_style method must therefore make a consistent choice from those inputs. The style-taking storage methods are implementation hooks, not per-vector overrides for selecting a different representation.\n\nArray storage results in HDF5 files where the dataset has the dimensions of each element, plus one added dimension. For instance, if each element to be stored is an m-by-n array, then the HDF5 file will contain an m-by-n-by-p array, where element k is [:, :, k].\n\nStructs can be stored in a \"portable\" way. For the a struct defined as:\n\nstruct MyType\n    a::Int64\n    b::Float64\nend\n\nthe resulting HDF5 file would look like so:\n\n/my_group/my_vector/arrays/a # a 1D array of Int64\n/my_group/my_vector/arrays/b # a 1D array of Float64\n\nThis format is called \"portable\" because it is easy to interpret this dataset outside of Julia.\n\n\"Portability\" is controlled by the portable keyword argument. When this is false, the above struct would be stored as:\n\n/my_group/my_vector # a 1D array of custom type inferred from MyType\n\nThis uses the HDF5 type system via the HDF5.jl package to encode the type. The underlying data can still be interpreted outside of Julia, but it requires substantially more code to interpret the type information in a useful way. If you are only interested in loading the HDF5 in Julia, use portable = false, and the resulting storage will be faster. (Note that non-bits types cannot use the HDF5 type system and hence will always use the portable form.)\n\nWhen the elements to be stored are themselves vectors, matrices, or arrays of known dimension, the user should provide those dimensions via the dims keyword argument. Otherwise, since the dimensions of an array are not known from its type, and it's not known if the user intends for dimensions to be consistent over time or not,\n\nKeyword arguments:\n\nportable: When true (the default), composite types like structs will be stored in a slower but more portable way. (For other types, this argument is ignored.)\ndims: Sets the dimensions of Array types (otherwise, ignored), such as (3, 4) when each element is a 3-by-4 matrix.\n\nUsers can add a storage_style method for their custom types to allow them to express how their types out to be stored. E.g., if a type should always be serialized, then this would instruct Julia to use serialization to a byte array for the give type:\n\nHDF5Vectors.storage_style(::Type{MyType}; kwargs...) = HDF5Vectors.ByteArrayStorageStyle()\n\n\n\n\n\n","title":"HDF5Vectors.storage_style"},{"category":"section","location":"custom_element_types/#Custom-Element-Types","page":"Custom Element Types","text":"Most types can be stored reasonably well as HDF5 vectors without having to specifying anything about how that should happen. However, sometimes it's desireable to have more control. In that case, it's possible to specify how a custom type should be stored in the HDF5 file, using one of the existing storage styles. The following methods should be implemented for the type:\n\nstorage_style\nconstruct\ndeconstruct\n\nHere's a complete example of a custom type for recording student grades, where the grade itself is stored as a string, where that string is going to be A, B, C, D, or F. Here is the native type:\n\nstruct Grade\n    label::String\nend\n\nHere's what's necessary to store this as the \"elemental\" style, where the label is stored as a char (UInt8).\n\nusing HDF5Vectors\nimport HDF5Vectors: storage_style, construct, deconstruct, ElementalStorageStyle, HDF5VectorOfElementalTypes\n\n# Tell it we want this stored using the \"elemental\" style, with UInt8 as the element type.\nstorage_style(::Type{Grade}; kwargs...) = ElementalStorageStyle(UInt8)\n\n# To store a Grade, pull the first (and only) char from the label string and convert to UInt8.\ndeconstruct(::Type{HDF5VectorOfElementalTypes{Grade, UInt8}}, el::Grade) = UInt8(only(el.label))\n\n# To rebuild a Grade from what was stored, make a string from the char.\nconstruct(::Type{HDF5VectorOfElementalTypes{Grade, UInt8}}, el::UInt8) = Grade(string(Char(el)))\n\nNow let's give that a try:\n\nusing HDF5\nh5open(\"custom_element_type.h5\", \"w\") do fid\n\n    # Create the vector.\n    arr = create_hdf5_vector(fid, \"grades\", Grade)\n\n    # Add some grades.\n    push!(arr, Grade(\"A\"))\n    push!(arr, Grade(\"B\"))\n    push!(arr, Grade(\"C\"))\n    push!(arr, Grade(\"D\"))\n    push!(arr, Grade(\"F\"))\n\n    # Show how that's stored in the file itself:\n    @show read(fid[\"grades\"][\"data\"])\n\n    # Show that in fact Grades are rebuilt from that data.\n    @show collect(arr)\n\nend\n\nThe resulting output is what we'd expect:\n\nread(fid[\"grades\"]) = UInt8[0x41, 0x42, 0x43, 0x44, 0x46]\ncollect(arr) = Grade[Grade(\"A\"), Grade(\"B\"), Grade(\"C\"), Grade(\"D\"), Grade(\"F\")]","title":"Custom Element Types"},{"category":"section","location":"custom_vector_types/#Custom-HDF5-Vector-Types","page":"Custom HDF5 Vector Types","text":"To create a new type of HDF5 vector, define a new storage style type (<:AbstractHDF5VectorStorageStyle), create the corresponding vector type (<:AbstractHDF5Vector), and define storage_style for the element types that use it. The style selection must be reproducible from the element type and stored options because it is performed again when loading a vector.\n\nThen implement the following HDF5Vectors storage hooks:\n\ncreate_hdf5_vector(style::MyNewHDF5VectorStorageStyle, group, name, el_type; kwargs...)\nload_hdf5_vector(style::MyNewHDF5VectorStorageStyle, group_or_dataset, el_type; kwargs...)\n\nThe generic copy_to_hdf5_vector implementation creates the selected vector and calls push! for each element. A storage backend may also implement a specialized bulk-copy method when it can do so more efficiently:\n\ncopy_to_hdf5_vector(style::MyNewHDF5VectorStorageStyle, group, name, collection; kwargs...)\n\nThese style-taking methods are implementation hooks. Application code should call create_hdf5_vector or copy_to_hdf5_vector without passing a style directly.\n\nThe vector type must also implement the following parts of the AbstractArray interface:\n\nBase.length(v)\nBase.push!(v, el)\nBase.getindex(v, k)\nBase.collect(v)\n\nBase.setindex!(v, el, k) is optional because some storage formats are append-only. If a vector type implements safe in-place replacement and may be used as a field of composite storage, also define HDF5Vectors.supports_setindex(::MyHDF5VectorType) = true.\n\nThese have definitions for AbstractHDF5Vector and likely don't need custom implementations:\n\niterable\nBase.eltype(v)\nBase.size(v)\nBase.similar(v, ...)\nBase.broadcastable(v)\nBase.map(f, v)\nBase.mapreduce(f, op, v; kwargs...)\nBase.iterate(v) and Base.iterate(v, state)\n\nIf your type uses construct and deconstruct, you'll need methods for those:\n\nconstruct(::Type{MyHDF5Vector}, el)\ndeconstruct(::Type{MyHDF5Vector}, el)\n\nFor an example of implementing a new type of storage, see the source for HDF5VectorOfCompositeTypes. It is fairly short, despite that it is used for the storage of all composite types in this package.","title":"Custom HDF5 Vector Types"},{"category":"section","location":"#HDF5Vectors","page":"Home","text":"This package provides a mechanism for storing vectors in HDF5 files rather than in RAM. Those vectors adhere to the AbstractVector syntax and can grow over time via push!. This can be particularly useful for long-running calculations, where the data that gets produced is simply too much to fit in RAM. It's also useful as a way to copy an array from Julia into an HDF5 file in a \"portable\" way that can be accessed outside of Julia, as well as a way to load those same vectors back into Julia.","title":"HDF5Vectors"},{"category":"section","location":"#Example","page":"Home","text":"Here is a simple example. We use HDF5 to open a file, and we use HDF5Vectors to create a vector in that HDF5 file.\n\nimport HDF5\nusing HDF5Vectors\n\n# Create an HDF5 file to store stuff in.\nfid = HDF5.h5open(\"storage.h5\", \"w\")\n\n# Create an array for Float64s called \"x\" in that file.\narr = create_hdf5_vector(fid[\"/\"], \"x\", Float64)\n\n# Push some elments into the array.\nfor el in 1. : 100.\n    push!(arr, el)\nend\n\n# We can now do whatever we want with that array, such as\n@show arr[end]\n@show sum(arr)\n@show collect(arr)\n\n# Always close out the file when you're done.\nclose(fid)\n\nWe can also create a new HDF5 vector based on existing storage in an HDF5 file.\n\nfid = HDF5.h5open(\"storage.h5\")\narr = load_hdf5_vector(fid[\"/x\"], Float64)\n\nFurther, you can move an existing array into HDF5 like so:\n\nfid = HDF5.h5open(\"storage.h5\")\nmy_hdf5_vector = copy_to_hdf5_vector(fid[\"/\"], \"some_name\", my_regular_vector)\n\nNote that writing into an HDF5 file element-by-element is much slower than keeping arrays in RAM, as is reading data back element-by-element, so HDF5Vectors should be used judiciously.","title":"Example"},{"category":"section","location":"#Supported-Types","page":"Home","text":"This works for storing many common types, including:\n\nElemental types:\n\nInt8, Int16, Int32, and Int64 (and unsigned forms)\nFloat32 and Float64\nEnum\nChar\nBits-type structs\n\nString-like types:\n\nString\nSymbol\n\nSingleton types:\n\nNothing\nImmutable zero-field marker types with zero-argument constructors\nEmpty tuples, named tuples, and static arrays\n\nArray-like types:\n\nSVector, SMatrix, and SArray of elemental type\nVector, Matrix, and Array of element type, where dimensions are constant from element to element\nNTuple of elemental type\n\nComposite types:\n\nGeneral tuple of types on this list\nGeneral named tuple of types on this list\nGeneral struct of types on this list\n\nSerialized types:\n\nVector, Matrix, and Array of non-elemental type or whose dimensions are not known in advance\nCustom types explicitly assigned ByteArrayStorageStyle or JSONStorageStyle\n\nSerialization provides a fallback approach for logging to HDF5 when other storage types do not make sense. For instance, a nonconcrete type, whose structure may change from element to element, defaults to serialization. This is slow and is intended only for types selected by the rules above. See \"Specifying a Storage Type\" below for more.\n\nPrimitive types that HDF5.jl does not natively support, including Float16, Int128, and UInt128, are rejected unless the user defines another storage style for them.","title":"Supported Types"},{"category":"section","location":"#Adding-Elements","page":"Home","text":"Values passed to push! must already be instances of the HDF5 vector's declared element type; HDF5Vectors does not convert them to that type. Elements stored with array-like storage must also have the dimensions declared when the vector was created.","title":"Adding Elements"},{"category":"section","location":"#Iteration","page":"Home","text":"When iterating over an HDF5Vector, it's far faster to call iterable on the vector and then iterate on what that returns. For example:\n\narr = create_hdf5_vector(...)\n...\n[el.x^2 + el.y^2 for el in iterable(arr)]\n\nThe reason this is faster is that iterable creates a structure intended to take advantage of the way HDF5.jl will access the data.","title":"Iteration"},{"category":"section","location":"#Replacing-Elements","page":"Home","text":"setindex! is available when the storage representation can replace an element in place. Byte-array serialization is append-only and does not support replacement. A composite vector supports setindex! only when every field does; otherwise, the operation throws an error before changing any field. Supported composite replacements are not transactional, so an unrecoverable HDF5 error can still leave some fields changed and others unchanged.","title":"Replacing Elements"},{"category":"section","location":"#Loading-an-Existing-Array","page":"Home","text":"Loading an HDF5 vector stored in an HDF5 file is straightforward:\n\nfid = HDF5.h5open(\"storage.h5\")\narr = load_hdf5_vector(fid[\"/x\"])\n\nWhere the type to load is known in advance, this works as well:\n\nfid = HDF5.h5open(\"storage.h5\")\narr = load_hdf5_vector(fid[\"/x\"], Float64)","title":"Loading an Existing Array"},{"category":"section","location":"#How-Data-Is-Stored","page":"Home","text":"","title":"How Data Is Stored"},{"category":"section","location":"#Elemental-Types","page":"Home","text":"These will simply be n-element arrays in the HDF5 file.","title":"Elemental Types"},{"category":"section","location":"#Array-Like-Types","page":"Home","text":"When the elements to be stored each have dimensions like (M, N, ...), the HDF5 file will have an array of the appropriate type whose dimensions are (M, N, ..., Z), where Z is the number of elements being stored. This is easy to interpret outside of Julia while also allowing fast access and efficient storage.\n\nWhen the elements to store are Vector, Matrix, or Array (or any AbstractArray whose dimensions are not known from the type), the dims = (M, N, ...) argument must be provided to create_hdf5_vector to use array-like storage. Otherwise, arrays cannot generally be stored this way and will instead be serialized to byte arrays, which is far slower and uninterpretable outside Julia.","title":"Array-Like Types"},{"category":"section","location":"#Composite-Types","page":"Home","text":"There are two ways to store a composite type: \"portable\" and \"non-portable\".\n\n\"Portable\" storage means that an HDF5 group is created for each field of the struct, and inside of each is a group/dataset.\n\nTo see how structs can be stored portably, imagine we want a vector of 100 MyType elements, where:\n\nstruct MySubType\n    c::Int64                # Elemental\n    d::NTuple{2, Float64}   # Array-like\nend\nstruct MyType\n    a::Float64              # Elemental\n    b::MySubType            # Composite\nend\n\nIf the HDF5 vector were created in the /my_group group with the name my_type, those would be stored portably like so:\n\n/my_group/my_type/               # Group\n/my_group/my_type/data/a         # Array of 100 Float64\n/my_group/my_type/data/b         # Group\n/my_group/my_type/data/b/data/c  # Array of 100 Int64\n/my_group/my_type/data/b/data/d  # Array of 2-by-100 Float64\n\nNote that a failed push! for a composite type may result in some fields of the composite type having updated values while others do not.\n\nFor bits-type structs, a user can specify that they want \"non-portable\" storage. This means that the HDF5.jl package can define a custom HDF5 type to store the struct, and the resulting HDF5 file will look like this:\n\n/my_group/my_type/ # An array of the HDF5 custom type\n\nThis is much faster and more efficient, but accessing it outside of Julia will require substantially more code to work with the HDF5 type definition system.","title":"Composite Types"},{"category":"section","location":"#Serialized-Types","page":"Home","text":"When a type is too flexible/odd for the above, it can be stored via serialization. This is far slower, but it works for almost everything. There are two types of serialization currently provided.\n\nThe ByteArrayStorageStyle uses Julia's Serialization package to serialize a given type to a byte array. The resulting HDF5 dataset will be uninterpretable outside of Julia.\n\nThe JSONStorageStyle uses the JSON3 package to serialize a given type to a JSON string. The resulting HDF5 dataset will be an array of JSON strings. See the example below. In order to use the JSONStorageStyle, your project will have to import JSON3.","title":"Serialized Types"},{"category":"section","location":"#Specifying-a-Storage-Type","page":"Home","text":"Users can specify what \"style\" of storage should be used for a given type. Storage style is a property of the element type and relevant creation options, not an override for one particular vector. The same storage_style method is called again when loading the vector, so it must make a consistent selection from those inputs.\n\nFor instance, suppose we had the following type:\n\n@enum ServerStatus unknown up down\nstruct SomeServerDetails\n    hostname::String\n    status::ServerStatus\nend\n\nWhen that's stored in the HDF5 file, let's make it serialize to JSON. To do this, we can apply the following trait:\n\nimport HDF5Vectors: storage_style, JSONStorageStyle\nstorage_style(::Type{SomeServerDetails}; kwargs...) = JSONStorageStyle()\n\nThat's it. Now, when we create an array for this type, each element will be serialized to JSON. The resulting HDF5 file will have an array of strings that can be loaded in any other environment. To complete the example:\n\nimport HDF5\nusing HDF5Vectors\nHDF5.h5open(\"server_details.h5\", \"w\") do fid\n    details = create_hdf5_vector(fid, \"details\", SomeServerDetails)\n    push!(details, SomeServerDetails(\"localhost\", up))\n    push!(details, SomeServerDetails(\"old_pc\", down))\n    push!(details, SomeServerDetails(\"phone\", unknown))\n    @show collect(details)\nend\n\nThe HDF5 file will have the following:\n\n/details/data/json/data # An array of JSON strings, one for each of the pushed elements\n\n(By default, the JSON is compact in style, not \"pretty\", to reduce the extra storage burden of all of those spaces. You can \"pretty\" on extraction from the HDF5 file if a human is supposed to look at it.)","title":"Specifying a Storage Type"}]
}
